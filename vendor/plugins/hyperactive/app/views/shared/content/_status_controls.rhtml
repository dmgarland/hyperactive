<% if !current_user.nil? and (current_user.has_role?("Admin"))  %>
    <p><label for="content_moderation_status"><%= 'Moderation status'.t %></label><br/>
    	<%= f.select 'moderation_status', %w(published promoted hidden) %>
	</p>
<% end %>