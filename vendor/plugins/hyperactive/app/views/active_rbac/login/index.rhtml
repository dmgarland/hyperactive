<h3>Your account</h3>

<p>
You are currently logged in as user '<%=h current_user.login %>'.
</p>

<% if current_user.has_permission?("see_admin_nav") %>
	<p>
		You are an administrative user. From here you can 
		<%= link_to "view the site admin pages", admin_main_url %>.	
	</p>
<% end %>

<% unless current_user.collectives.empty? %>
	You belong to the following group(s): 
	<% current_user.collectives.each do |c| %>
		<%= link_to c.name, c %>
	<% end %>
<% end %>

<p>You may log out at the <%= link_to "logout", logout_url %> page.</p>