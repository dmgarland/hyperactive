<% tag_list.split(" ").each do |tag| %>
    <% url = tag_item_path if tag_type == "tag" %>
    <% url = place_tag_item_path if tag_type == "place"%>
    <%= link_to(h(tag), "#{url}#{tag}") %> 
<% end %>