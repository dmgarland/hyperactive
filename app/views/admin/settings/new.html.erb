<h2>New setting</h2>

<% form_for(@setting, :url => admin_settings_url) do |f| %>
<%= f.error_messages %>

<%= render :partial => 'form', :locals => {:f => f} %>
<p>
<%= f.submit "Create setting" %>
</p>
<% end %>

<p>
<%= link_to 'Back to settings', admin_settings_url %>
</p>