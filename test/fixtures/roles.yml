admin:
  title: Admin
  static_permissions: [hide, edit_all_content, see_admin_nav, destroy, feature_content, promote_content, edit_all_groups, see_admin_note]

anonymous:
  title: Anonymous

registered:
  title: Registered
  static_permissions: [hide_own_content, edit_own_content, hide_own_content]

hider:
  title: Hider
  parent: registered
  static_permissions: [hide]

auto_promote:
  title: AutoPromoted
  static_permissions: [auto_promote_content]

moderator:
  title: Moderator
  parent: registered
  static_permissions: [promote_content, hide]

