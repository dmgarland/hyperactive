<h1>Listing feeds for <%= @group.name %></h1>

<table>
  <tr>
    <th>Title</th>
	<th>Url</th>
    <th>Summary</th>
  </tr>
  
<% for feed in @external_feeds %>
  <tr>
    <td><%=h feed.title %></td>
    <td><%=h feed.url %></td>
    <td><%=h feed.summary %></td>
    <td><%= link_to 'Edit', edit_group_external_feed_path(@group, feed) %></td>
    <td><%= link_to 'Destroy', group_external_feed_path(@group, feed), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New external feed', new_group_external_feed_path(@group) %>