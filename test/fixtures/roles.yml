admin:
  title: Admin
  static_permissions: [hide, edit_all_content, see_admin_nav, destroy, feature_content, promote_content, edit_all_groups]

anonymous:
  title: Anonymous
  
registered:
  title: Registered
  static_permissions: [hide_own_content, edit_own_content, hide_own_content]
  
hider:
  title: Hider
  parent: registered
  static_permissions: [hide]

auto_promote:
  title: AutoPromoted
  static_permissions: [auto_promote_content]
