<% if editable_by?(current_user, @content)  %>
<p>or</p>
<div>
	<strong>Edit this <%= @content.class.to_s.downcase %></strong>
	<p><%= link_to 'Edit'.t, :controller => @content.class.to_s.downcase.pluralize, :action => 'edit', :id => @content %></p>
</div>
<% end %>